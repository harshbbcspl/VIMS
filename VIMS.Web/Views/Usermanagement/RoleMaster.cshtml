@model IEnumerable<VIMS.DB.VIMS_RoleMasterRtr_Result>

@{
    ViewBag.Title = @VIMS.Model.Resources.Role.title;
    ViewBag.Module = "User Management";
}

<div class="row">
    <div class="col-sm-12 my-5">
        <div class="card card-table">
            <div class="card-header row cardTitleRow">
                <div class="col">
                    <h4 class="card-title float-left">@VIMS.Model.Resources.Role.RoleMaster</h4>
                </div>
                <div class="col-auto">
                    @*@if (Convert.ToInt32(TempData["InsertRight"]) == 1)
        {*@
                    <a href="@Url.Action("AddRole", "Usermanagement")" class="btn btn-outline-primary float-right">
                        @VIMS.Model.Resources.Role.AddRole
                    </a>
                    @*}*@
                    
                </div>
            </div>

            <div class="card-body booking_card">
                <div class="table-responsive">
                    <table class="table card-table table-vcenter text-nowrap datatable">
                        <thead>
                            <tr>
                                <th>@VIMS.Model.Resources.Role.Action</th>
                                <th>@VIMS.Model.Resources.Role.Active</th>
                                <th>@VIMS.Model.Resources.Role.RoleName</th>
                                <th>@VIMS.Model.Resources.Role.CreatedBy</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @*@if (Convert.ToInt32(TempData["UpdateRight"]) == 1)
        {*@
                                        <a href="@Url.Action("EditRole", "Usermanagement", new { id = item.RoleId })" class="mx-3">
                                            <i class="ti ti-edit fa-2x"></i>
                                        </a>
                                        @*}
        else
        {
            <i class="ti ti-edit-off fa-2x"></i>
        }*@
                                        
                                    </td>

                                    <td class="justify-content-center d-flex">
                                        @*@if (Convert.ToInt32(TempData["DeleteRight"]) == 1)
        {*@
                                        <div class="slider">
                                            <input type="checkbox" name="slider" class="slider-checkbox" id="@item.RoleId" onclick='UMActiveDeactiveStatusChange(this, "@item.RoleId", "RoleMaster")' @(item.IsActive == true ? "checked" : "")>
                                            <label class="slider-label" for="@item.RoleId">
                                                <span class="slider-inner"></span>
                                                <span class="slider-circle"></span>
                                            </label>
                                        </div>
                                        @*}
        else
        {
            <div class="slider">
                <input type="checkbox" name="slider" class="slider-checkbox" disabled="disabled" id="@item.RoleId" onclick='UMActiveDeactiveStatusChange(this, "@item.RoleId", "RoleMaster")' @(item.IsActive == true ? "checked" : "")>
                <label class="slider-label" for="@item.RoleId">
                    <span class="slider-inner"></span>
                    <span class="slider-circle"></span>
                </label>
            </div>
        }*@
                                    </td>

                                    <td>@item.RoleName</td>
                                    <td>@item.CreatedBy</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>